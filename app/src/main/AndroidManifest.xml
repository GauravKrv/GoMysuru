<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.karve.gomysuru1">

    <application
        android:allowBackup="true"
        android:icon="@drawable/stock_vector_mysore"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GoMysuru1">
        <activity android:name=".ui.description.RecreationDescription"></activity>
        <activity android:name=".ui.description.PhraseList" />
        <activity android:name=".ui.description.TourguideDescription" />
        <activity android:name=".ui.contacts.ContactList" />
        <activity android:name=".ui.description.CuisinesDescription" />
        <activity android:name=".ui.description.FestivalsDescription" />
        <activity android:name=".ui.description.ArtDescription" />
        <activity android:name=".ui.description.DescriptionActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.GoMysuru1.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>